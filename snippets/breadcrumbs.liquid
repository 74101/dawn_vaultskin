<nav class="breadcrumbs" aria-label="breadcrumbs">
  <ul>
    <li>
      <a href="/">Home</a>
    </li>
    /
    <li>
      <a href="/collections/all">All Products</a>
    </li>
    /
    {% if product.metafields.products.primary_collection.value != blank %}
      {% assign primary_coll = product.metafields.products.primary_collection.value %}
      <li>
        <a href="{{ primary_coll.url }}">{{ primary_coll.title }}</a>
      </li>
      /
    {% endif %}
    <li aria-current="page">
      {% if product_collection %}
        <h1 class="collection-hero__title">
          <span class="visually-hidden">{{ 'sections.collection_template.title' | t }}: </span>
          {{- collection.title | escape -}}
        </h1>
      {% else %}
        <a href="{{ request.path }}" aria-current="page">{{ item_title }}</a>
      {% endif %}
    </li>
  </ul>
</nav>